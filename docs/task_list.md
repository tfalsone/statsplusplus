# Task List

Open work items. Completed items are in `docs/changelog.md`.

---

## Bugs — CLI Scripts

Discovered during full script verification pass.

- [x] **`trade_calculator.py` crashes on ALL MLB players** — `value_player()` line 128: `fv_continuous = pe["fv_continuous"] or float(fv_int)`. MLB players have `fv=None` and `fv_continuous=0.0` (falsy). The `or` branch tries `float(None)` → TypeError. **Fixed:** Check `fv_continuous` truthiness separately from `fv_int is not None`. **Done Session 79.**
- [x] **`trade_calculator.py` JSON format `--trade` broken** — The `{"my_team_send": [{"name": "..."}]}` input format crashes with `KeyError: 'player_id'` because the parser expects a `player_id` key in each spec dict. **Fixed:** `evaluate_trade()` now resolves `{"name": "..."}` entries via `resolve_player()` before calling `value_player()`. **Done Session 79.**
- [x] **`prospect_value.py --player <id>` broken** — `find_player()` calls `db.get_conn()` which doesn't exist. **Fixed:** Uses `get_connection(get_league_dir())` from proper imports. **Done Session 79.**
- [x] **`farm_analysis.py` broken** — Line 115 references `_db.get_conn()` but `_db` is never imported. **Fixed:** Uses the existing `conn` variable already open in the function scope. **Done Session 79.**

---

## Bugs — Web UI

- [x] **Historical MiLB stats show "L0" / wrong level** — Stat rows played in leagues that were later renamed/promoted/removed (leagues get reorganized over the years) had no `league_id → level` mapping, since `league_settings.json`'s `minor_leagues` is a current-snapshot only. **Fixed Session 87:** refresh now writes a cumulative `milb_league_map` (merges rather than overwrites); player-page MiLB career table reads it and labels unresolvable leagues "MiLB" instead of "L0". Shared accessor `milb_league_map()` in `web_league_context.py`.
- [x] **Percentile year/level dropdown skips seasons** — Seasons played in orphaned (removed) leagues were silently dropped from the percentile dropdowns (Steve Murphy, PPL: 1952/1953 missing). **Fixed Session 87:** `percentiles.py` groups every stat-referenced `league_id` that can't resolve to a real level under a synthetic DB-sourced "MiLB" bucket (`UNKNOWN_MILB_LEVEL`). Tests: `tests/test_pctile_levels.py`.
- [x] **Advanced-tab percentile ordering flips on handedness split** — The server-rendered "Percentiles by Season" table sorted oldest-first while the JS split (vs L/vs R) view sorted newest-first, so toggling flipped the order. **Fixed Session 87:** per user preference, all stat views now sort year **ascending** (oldest at top, newest at bottom) and agree — batting/pitching Stats tables, vs L/vs R split tables, advanced percentile table, JS split view, and fielding percentile history.

**Follow-up (not done):** the advanced-tab initial (all-levels) table has a Level column; the JS-rendered handedness-split table is MLB-only and has no Level column — a remaining structural inconsistency between the two renderings, separate from the ordering fix. **LOE: Low.**

---

## Code Quality

### StatsPlus API integration (Session 83)

### Dynamic Pages (Session 85)

- [x] **Offseason page (Phase A / proof-of-concept)** — First phase-aware "dynamic page" (`/offseason`, `web/offseason_queries.py`, `offseason.html`). Manual toggle (Settings → Dynamic Pages, `offseason_mode` in state.json) + a full-width phase stepper (`offseason_phase`) that gates panels via `panels_for_phase`. Panels: Arbitration (perpetual-arb-aware, `$/WAR`-scaled tender recs), Free Agency market board (unsigned + league-played FAs only, position/age/needs filters, ★ need-fill flag via `get_draft_org_depth`, Proj WAR from the shared `compute_player_value`), Extension candidates, always-on Trades. Endpoints `/api/toggle-offseason`, `/api/set-offseason-phase`. Tests: `tests/test_offseason.py` (11). **Done Session 85.**
- [ ] **Offseason page — auto-enablement + sub-phase auto-detection** — Decision (user, Session 85): keep the offseason page a **manual toggle** while it matures; move to automatic once further along. The detection mechanism now exists — `team_queries._determine_phase` (data-driven, league-adaptive, reads `games.game_type` boundaries + last-played-game recency) reliably returns Regular Season / Postseason / Offseason / Spring Training. Two pieces to wire when ready: (1) **auto-enable** offseason mode when `_determine_phase` == "Offseason" (keep the manual override for early testing / edge cases); (2) **sub-phase** auto-detection for the stepper — the top-level phase gets us to "Offseason," but the finer stations (Arbitration → Options → FA → Rule 5 → Spring) need data-transition signals (a wave of `free_agent=1` flips = FA opened; arb-settlement salary changes; Rule 5 draft completion). OOTP's own phase label isn't in the API and is coarse. Keep the manual stepper as override. **LOE: Medium.**
- [ ] **Offseason page — Spring panel** — The stepper's Spring station still shows a "coming soon" placeholder (the only remaining placeholder — **Season Review DONE Session 89**, replacing the old Playoffs placeholder). **Rule 5 panel DONE Session 87** — `get_rule5(team_id)` (protect + draft-target sides) built on the confirmed `years_protected_from_rule_5` semantics (0 = eligible this offseason), gated via `panels_for_phase`; `draft_eligible` confirmed to be amateur-draft eligibility, not Rule 5. Spring panel: scope TBD (roster battles / NRI / depth-chart finalization). (Options panel done Session 86b.) **LOE: Medium.**
- [ ] **Offseason page — trade enrichment (future scope)** — The Trades panel currently just links to existing tools. The vision (user, Session 85): combine the trade tools with contextual awareness of what each team needs (buyer/seller classification, what a seller should move, what fills the user's needs) so trade suggestions are situation-aware. Build for the offseason page, then reuse for a future Trade Deadline dynamic page. Separate scoping/design session. **LOE: High.**
- [ ] **Offseason page — FA market board rework (consume the budget)** — Session 86 built the finance scaffolding (`finance_settings` v2 + Offseason Budget panel) and the **FA targets cart** (Session 86b): each FA row has a value-based recommended contract, "+ Target" adds to a localStorage per-league cart that draws down `fa_budget` by AAV, with per-target override. **Remaining:** needs-first positional-grid front door (roster holes → best available FAs per hole), value-vs-cost visual context. Reference patterns: FanGraphs RosterResource, Spotrac payroll timelines, fan "be-the-GM" offseason simulators. **LOE: Medium.**
- [ ] **Offseason page — recommended contract refinement (demand + pipeline-aware length)** — The cart's cost estimate is a value proxy: `max(proj_war × $/WAR, min_salary)` (min-salary floor added Session 86b), age-curve length, labeled "est." — and it also shows on the player page Surplus Projection panel for FAs (single source of truth: `finance_settings.recommended_contract`). Refine it: market tax (FAs pay above pure surplus), positional scarcity, and a **recommended length that accounts for the farm pipeline** — don't recommend a 5-yr deal at a position where a AA prospect is 1-2 years from filling the hole. Reuses `get_draft_org_depth` + prospect ETA. No reference tool does the pipeline-aware length piece — it's the original differentiator. The API does NOT expose FA demand (confirmed Session 86b: reviewed `/players`, `/contract`, `/draftpool`), so this stays a computed estimate + user override. `recommended_contract`'s return shape is fixed so the richer model drops in without UI changes. **LOE: Medium.**
- [ ] **Move `draft_settings` / `finance_settings` consistency** — `finance_settings` correctly lives in the package (`src/statsplusplus/config/`, pure logic per the convention). `draft_settings.py` is still in `scripts/` despite being pure logic with no CLI — a pre-existing inconsistency. Move it to `src/statsplusplus/config/` (or a `settings` subpackage) and update the ~4 import sites (`draft_board`, `api_routes`). Low-risk mechanical refactor. **LOE: Low.**
- [ ] **Web-test fixture isolation for multiple live-app modules** — `web/app.py` runs `init_schema` over all of `data/*` at import, and web tests build/tear down fixtures in the real `data/` dir. A second live-app test module (`test_offseason.py`) driving the app via the test client contaminated `test_web_smoke.py` (shared cached `app` + `data/` scan). Worked around by testing offseason query logic via patched `get_db`/`get_cfg` and NOT adding `/offseason` to the smoke route list. Proper fix: isolate web fixtures (temp data root per module, or a fresh app factory per module) so any web module can drive the live app safely — then add `/offseason` to smoke. **LOE: Medium.**

- [x] **API token authentication** — Sanctioned per-team token as the preferred auth method, cookie fallback, content-type/human-message guard in `_fetch` (raises on expired/invalid token), `/tokencheck`-backed Test Connection, threaded through all `configure()` sites, UI in Settings + onboarding + the header session panel. **Done Session 83.**
- [x] **Refresh `/date` gate + `/ratings` cooldown handling** — skip redundant pulls when the game date is unchanged (`--force` to override); surface the once-per-5-min `/ratings` cooldown as `RateLimitedError` instead of blocking. **Done Session 83.**
- [x] **PR #10 (fokoba) — closed (already addressed)** — "Fix trade valuations using the wrong league's data when switching leagues." The stale-singleton it fixes in `contract_value.py` is no longer reachable from the long-running web server: the Session 78 unified-evaluation refactor routed the web trade path (`web/trade_queries.py get_trade_value`) off `contract_value.py` entirely — it now reads `player_evaluation`/`player_surplus` directly. `contract_value` is only imported by `scripts/validate_unified.py` (a one-shot script). Closed on GitHub as superseded by the Session 78 decoupling. **Verified/closed Session 83.**
- [x] **PR #11 (fokoba) — closed** — the token-auth PR was adapted into the Session 83 work (with the token-expiry/content-type safety it lacked), shipped in v1.5.0. Closed on GitHub as merged/superseded. **Done Session 83.**
- [ ] **`/date`-gate force option in the web UI** — the CLI has `--force`; a redundant web Refresh click now shows "Already up to date." If users want to force a re-pull from the browser, add a force affordance (e.g. shift-click or a small menu). Low priority — fix-and-retry after a failure already re-runs without force. **LOE: Low.**
- [ ] **OSA ratings pull (`/ratings?osa=1`)** — The wiki confirms `?osa=1` returns the OSA (scout-independent) ratings, is **anonymous** (no token/cookie needed), and is rate-limited per-IP (once per 15 min). This unlocks **scouted-vs-OSA comparison** across the whole league. Potential uses (per user): flag players whose team's scouting diverges sharply from OSA (over/under-scouted), a "true talent" second opinion independent of the user's own scouts' accuracy, and cross-org draft/trade evaluation on a common baseline. Implementation: add an `osa=1` fetch path to the client (anonymous — no auth headers), store OSA ratings in a parallel table or as `*_osa` columns, surface divergence in player/prospect pages and the draft board. Sequencing: pull + store first, then a divergence UI. **LOE: Medium.** *(Supersedes the earlier Phase 5a roadmap stub.)*
- [ ] **StatsPlus API doc-diff review (recurring)** — Systematically compare the current API wiki (`docs/StatsPlus APIs _ StatsPlus Wiki.html`, saved Session 83) against what the client/refresh actually consume, to find newly-documented endpoints/fields and feature opportunities. Known endpoints from the wiki: `/teams`, `/players`, `/date`, `/tokencheck`, `/contract` + `/contractextension`, `/exports`, `/gamehistory`, `/teambatstats` + `/teampitchstats`, `/playerbatstatsv2` + `/playerpitchstatsv2` + `/playerfieldstatsv2`, `/draftv2`, `/draftpool`, `/tradeblock`, `/ballparks`, `/lgdata`, `/ratings` (+ `osa=1`). Audit which we use, which fields within each we ignore, and what new data enables. **LOE: Medium (investigation).**
- [x] **New `/players` fields (Session 83 audit)** — `/contract` and the stat endpoints are fully covered; several `/players` fields (added April/July 2026) now handled. **DONE Session 87:** (1) `Organization ID` — stored + canonical org attribution via `db.ORG_ID_SQL`; (2) `?retired=0` — steady-state refresh filter (full pull on first refresh, active-only after; `--full` to force); (3) `League ID` — stored as `player_league_id`, drives intl-complex (level=8) reclassification (negative = intl); (5) `years_protected_from_rule_5` / `draft_eligible` — stored, semantics confirmed against vMLB data (ypr 0 = Rule 5-eligible; `draft_eligible` = amateur-draft, dormant), and consumed by the Rule 5 offseason panel. **Decided-skip:** (4) `bats`/`throws` from `/players` — redundant with the ratings source consumed everywhere via `latest_ratings`. Lower value / not planned: `secondary_service_*`, `last_team_id`, `hall_of_fame`/`inducted`, `draft_supplemental`. See `docs/statsplus_api_analysis.md` §7. **Done Session 87.**
- [ ] **`/date`-driven per-endpoint caching (extension)** — beyond the refresh gate, individual non-`/date`-changing endpoints (teams, players, contracts, ballparks, lgdata) could be cached and skipped between refreshes. Lower value now that the whole-refresh gate exists. **LOE: Medium.**
- [ ] **Historical team-stats backfill — reduce first-refresh wall-clock (deep leagues)** — Investigated Session 89 (from a PPL first-refresh incident). A deep retro league (game year 1955) spent ~40 min on the first refresh: the team-stats render endpoints (`/teambatstats`, `/teampitchstats`) are limited to **1 render/min/caller**, and the 15-year historical backfill renders ~2 per year × 15 = ~30 minute-gated renders. Session 89 fixed the *reliability* (proactive render pacing in `_fetch` → no more 429-storm + wasted requests + ratings-ID expiry; re-request on expiry) and confirmed the backfill already **skips already-fetched years**, so this is a **one-time first-pull cost** (subsequent refreshes re-render only current + prior year, ~2 min). But the first pull is still inherently minute-gated. Options to explore for a faster/less-blocking first refresh: (1) make deep historical team-stats backfill **optional or lazy** — historical team stats feed `$/WAR` and long-term context, NOT the core current-season evaluation, so the first refresh could defer them and populate the app immediately, backfilling in the background / on a later pass; (2) a **background/scheduled** refresh so the wall-clock is invisible; (3) confirm whether player-stat endpoints (`/playerbatstatsv2` etc.) share the same 1/min render limit or are cheaper (the log shows player-stat pulls were fast — only team-stats 429'd), and whether we even need per-year *team* stats vs deriving them from stored player stats. **LOE: Medium.**



- [x] **Additional ratings scales** — Support 1-20 scale (maps to 20-80 via linear `20 + (raw-1)/19 * 60`). Auto-detection: max rating ≤20 → 1-20, >80 → 1-100, else 20-80. Added to settings and onboarding dropdowns. **Done Session 57.**
- [x] **Snapshot test fragility** — `test_prospect_value.py` now stubs `dollars_per_war()` and `league_minimum()` via `unittest.mock.patch` to fixed values ($7M, $800K). Tests are fully deterministic regardless of `league_averages.json` state. Structural invariants (monotonicity, option ≥ base, SP > RP) plus a $/WAR scaling test. `test_player_utils.py` was already stable (exact FV grades depend on model_weights.json which only changes during recalibration, not refresh). **Done Session 57.**
- [x] **Evaluation engine docs** — Add `evaluation_engine.py` to `docs/tools_reference.md` and `docs/system_overview.md`. Document the `run()` entry point, pure computation functions, and the batch pipeline integration in `refresh.py`. **LOE: Low.**
- [ ] **Comprehensive regression testing** — Current test suite has structural gaps: (1) `test_evaluation_pipeline.py` tests hitter stat-blending end-to-end but not pitcher stat-blending (caught by missing-ERA bug, Session 67), (2) depth chart tests are smoke tests only ("returns dict?") with no behavioral assertions for edge cases (NULL OVR, failed evaluation engine), (3) `_league_pos_rankings` has zero direct test coverage, (4) no integration tests that simulate a league without OVR ratings (PPL-like scenario). Need: systematic regression test pass covering all major features with both happy-path AND edge-case assertions — especially cross-league scenarios (different scales, missing data). Priority areas: evaluation pipeline (all player types × stat blending), depth chart (fallback chains, future-year projections), surplus model, and trade calculator. **LOE: Medium-High.**

  **Added Session 80 — interface/packaging coverage gap:** the suite passed while `spp-refresh`/`spp-calibrate` entry points were broken (no `main()`), `scripts/refresh.py`/`scripts/calibrate.py` shims were missing, and a header-repair bug silently corrupted ratings on a new CSV format. Nothing exercised: (1) every `pyproject.toml` entry point resolves to a callable, (2) documented `scripts/*.py` invocations import and run, (3) the live Flask app (`web/app.py`) boots and its key routes return 200 against a fixture DB, (4) the ratings CSV header handler across known formats. **Mostly addressed Session 80:** `tests/test_entry_points.py` covers (1), `tests/test_cli_smoke.py` covers (2) by running every CLI tool against OVR-present and OVR-less fixture leagues, `tests/test_web_smoke.py` covers (3) by booting the live `web/app.py` against fixture leagues and asserting key routes return non-5xx, and `tests/test_ratings_header.py` covers (4). Shared fixture builder in `tests/_fixture_league.py`. This layer caught three additional bugs during authoring (benchmark prospect path, team.html div-by-zero, projections NULL games).
- [ ] **Release pipeline implementation** — Design in `docs/testing_pipeline_design.md` (Session 81). Staged plan, in priority order: (1) **CI workflow** — run pytest + mypy on a Python 3.10/3.11/3.12/3.13 matrix on every push/PR (nothing runs the suite automatically today; highest-value missing piece). (2) **Artifact-boot validation** — build the release zip, extract in a clean container, run the launcher install path, assert the app boots (would have caught the `start.sh` install/shim bugs). (3) **Playwright rendering check** — headless drive of key pages asserting no console errors / elements render. (4) **Live-API contract canary** — scheduled/manual job with a real cookie validating the StatsPlus ratings-CSV format + endpoint shapes, alerting on drift before a refresh corrupts data. **LOE: Medium (stages 1–2), Medium-High (3–4).**
- [x] **Composite function consolidation** — `compute_composite_hitter` and `compute_composite_pitcher` now have a single canonical implementation in `evaluation/composite.py`. `data/evaluation_engine.py` imports from the package (was maintaining identical copies). Also consolidated: all helper functions (tool_transform, sub_mlb_floor_penalty, offensive/baserunning/defensive raw, compensation, stat_to_2080, pitcher_stat_to_2080, combined_value) and ceiling functions (compute_ceiling, compute_true_ceiling, compute_component_ceilings). Fixed PITCHER_TOOL_KEYS bug: package version now includes hra/pbabip (was missing). Eval engine reduced from 3493 → 2450 lines. **Done Session 79.**
- [x] **Model parameter calibration from data** — All tuning parameters extracted to `constants.py` with per-league override via `ModelWeights.get_param()`. Regression testing script (`scripts/model_regression.py`) validates composite vs WAR and calibrates aging curves + imbalance thresholds per-league. Defense weights recalibrated (SS/2B 0.05→0.15, 3B 0→0.10). Speed×contact synergy bonus added. Position-specific stat blend reduction for defense-first positions. `calibrate.py` fully rewritten: peak-age regression (27-32), WAR target, no floors, adaptive age range, HRA as pitcher movement proxy. EMLB hitter R²=0.667, VMLB hitter R²=0.397 (beats OVR). See `docs/evaluation_model_findings.md`. **Done Session 79.**

---

## Evaluation Engine Tuning

- [x] **Run-space facet evaluation model (hitters) — CORE SHIPPED Session 92** — Full design in `.kiro/specs/run-space-facet-model/`. Hitter value is now built additively in **runs** (bat wRAA + baserunning-UBR-runs + fielding-ZR-runs + positional adjustment → OOTP-WAR-anchored WAR → 20-80 composite), replacing the old grade-space share-weighted blend. New pure `evaluation/facet_runs.py`; per-league run-space calibration in `calibrate.py` (`_calibrate_run_space` → wOBA scale, br/def curves per position, OOTP-WAR anchor, runs→composite mapping, tool→wOBA fit, wOBA weights; persisted to `tool_weights.json`); threaded through `compute_composite_hitter` (graceful grade-space fallback) and the batch engine. **Per-facet stat blend (convergence):** MLB hitters blend observed career wRAA/UBR/ZR per facet by facet-specific stabilization confidence (bat slow ~600 PA, baserunning fast ~250 PA, fielding slowest ~900 IP), replacing the OPS+ `compute_composite_mlb` blend so composite & WAR share one run total; prospects blend level-relative MiLB wRAA + UBR (defense tool-only — MiLB fielding unavailable), Step-2 MiLB OPS+ blend guarded off to avoid double-count. WAR fidelity: new-WAR~OOTP-WAR R² 0.70-0.84 (vs ~0.10 old). Validated: full suite 965 passed; Cubs re-ranked sensibly (up-the-middle/catchers up, weak-glove corner bats down); prospect FV distribution healthy. **PPL recalibrated; eMLB/vMLB pending calibrate+fv_calc.** Pitchers unchanged (out of scope v1).
  - [x] **FOLLOW-UP: per-facet aging + development projection — DONE Session 92.** `compute_player_value` now ages each facet on its own curve (bat latest, baserunning earliest/steepest, defense moderate) weighted by run-share (P1); projects prospect development along the age-based BAT curve (P2); and personalizes the development ramp by the player's prior dev_speed z-score as a clamped [0.6,1.4] modifier (P3) — fast developers accrue surplus earlier, stalled ones later, with **FV/ceiling unchanged** (dev_speed stays an independent axis; timing-only). Spec: `.kiro/specs/per-facet-aging-projection/`. Tests: `test_facet_aging.py`, `test_facet_development.py`.
  - [ ] **FOLLOW-UP: pitcher run-space model.** Pitchers stay on the composite→WAR path (out of scope v1). A pitcher run-spine (FIP/RA9 run components) is a later phase. **LOE: Medium-High.**
  - [ ] **FOLLOW-UP: per-league survivorship-corrected aging/dev curves.** The per-facet aging (`AGING_*`) and development (`DEV_*`) curve SHAPES are hardcoded literature priors, identical across leagues — only the facet run *magnitudes* they multiply are per-league. Deliberately NOT fit from per-league data yet: the cross-section is survivorship-biased (observed UBR *rises* at 33+ because only good baserunners still get 300+ PA — would wrongly imply baserunning improves with age) and the longitudinal sample is too thin (same-player YoY facet transitions n=10-19/age band; ~7 `ratings_history` snapshots over ~1yr). Proper per-league fit needs the **delta/matched-pairs method with survivor+dropout weighting** AND multi-season depth. Gated on accumulated history — as `ratings_history` deepens, fit per-league facet aging/dev curves (survivorship-corrected) and shrink toward the current priors by sample size. **LOE: Medium-High. Gated on data.**
  - [ ] **FOLLOW-UP: dev_speed transitional-window artifact + recalibration.** `dev_speed` reads composite deltas across `ratings_history` snapshots, but only the CURRENT snapshot's `composite_score` is recomputed each run — older snapshots keep their original **grade-space** composite while the latest is **run-space**. So a window straddling the migration measures the MODEL CHANGE as development. Bounded: only ~1.7% of eMLB rows show implausible |annual_move|>15 (mean move +1.4, sd 6.2 — most players' run-space vs grade-space composite is close), but the divergent players (e.g. a 65→45 "regression" that's really the model shift) are spurious. Self-heals as post-migration run-space snapshots accumulate and age out the grade-space ones (~1 year). Options: (a) accept and let it heal; (b) backfill historical `ratings_history.composite_score` under the run-space model; (c) gate dev_speed windows to post-migration snapshots. Also recalibrate benchmarks against the new composite. **LOE: Low-Medium.**
  - [x] **FOLLOW-UP: scratch harness cleanup — DONE Session 92.** The validation scratch tools (`scripts/run_space_baseline.py`, `run_space_prospects.py`, `run_space_mlb_facets.py`) were removed at release; the calibration logic they prototyped now lives in `calibrate._calibrate_run_space`.
  - [x] **FOLLOW-UP: defensive ratings → fielding runs (range-sufficient) — RESOLVED Session 93.** Question (from a prospect spot-check): should run-space fielding runs use the full defensive profile (arm IFA / hands IFE / DP TDP) rather than the single range tool (IFR/OFR)? Tested on eMLB: range→ZR r=0.559; full-glove (range+arm+hands+dp)→ZR only **+0.014** better; and arm/hands/DP are *negatively* associated with WAR after controlling for range (partial r −0.215) — a positional-selection confound (weak-range players kept at premium spots carry compensating secondary tools, so conditioning on range flags the LESSER overall defender). The `ss`/`c_arm`-inclusive game positional grade is a *worse* ZR predictor (0.338) than range alone. **Conclusion: the range-only per-position ZR curve is empirically correct and sufficient; do NOT add secondary defensive tools (would not help ZR prediction and would degrade WAR prediction).** The single caveat is that ZR itself captures only the range dimension — error/DP run *events* aren't modeled — but the ratings that would proxy them have no positive WAR signal here, so there's nothing reliable to add. Closes the standing "how defensive ratings feed into evaluation" question.
  - [x] **FOLLOW-UP: FV rounding cliff + composite-mapping population-centering — DONE Session 93.** (1) FV snap-to-5 no longer floors high-gap prospects while rounding near-ceiling ones (the gap discount already lives in `fv_continuous`); consistent rounding removes a full-grade cliff on upside. (2) `comp_mapping` center anchored on the population instead of selective 300+ PA starters (was inflating grades ~1 pt, worst in eMLB). See changelog.
  - [ ] **FOLLOW-UP: position-relative context on the player page (output).** The single most clarifying lens this session was expressing a player's bat/defense as a percentile/z RELATIVE TO HIS POSITION (a −12 wRAA/600 SS bat looked alarming in absolute terms but was only z −0.27 for a light-hitting-SS league; range IFR 78 was P59 among MLB SS, not "elite"). None of this is surfaced in the app — a user sees composite/FV but no position-relative breakdown. Add position-relative percentiles (projected bat, range, speed, per-facet run contributions) to the player page so users can self-serve the "is this grade right?" analysis. Model-safe (pure display), high analytical value, directly serves output quality. **LOE: Medium.**
  - [ ] **FOLLOW-UP: residual-bat term — known-small, folded into composite↔WAR work.** After controlling for composite, projected bat retains a modest independent WAR signal (partial r 0.167, ~0.031 WAR/run). A standalone calibrated residual-bat FV adjustment improved WAR alignment only +0.004 corr while flipping ~2,500 grades via rounding noise — **not worth shipping standalone.** Revisit only inside the composite↔WAR alignment project below, as one small term among others, validated on the hold-out. **LOE: folded into the item below.**
  - [x] **Sub-item — offense target → wOBA (Phase 0, DONE Session 92).** `_calibrate_tool_weights` regresses offensive tools against per-player **wOBA** (per-league/year run-env-derived weights, canonical fallback, sample guards) instead of total WAR. Inverts the backwards ordering: gap-dominant→power-dominant. `evaluation/woba.py` + 8 tests. Shipped as part of the run-space model.
  - Original finding (Session 91): offensive tools regressed against total `war` bundled defense/baserunning/positional value → gap dominant (positional confound). PPL n=833: vs WAR gap 0.43/power 0.19; vs OPS power 0.43/gap 0.23. Orthogonal to / predates the NPB fix.







- [ ] **NPB/cross-league scoping — revisit the `player_league_id IS NULL` allowance** — Session 90 fixed NPB (a co-resident `level=1`, non-primary league, `player_league_id=228`) contaminating PPL's evaluation engine by scoping "MLB" to the primary league via `primary_league_predicate(primary_league_id)` — which keeps players with `player_league_id = primary OR player_league_id IS NULL`. The `IS NULL` allowance is required for backward-compat (single-top-league DBs / older data where the field isn't populated) but is a soft edge: PPL had ~5 level-1 players with NULL `player_league_id` that the allowance lets through as "MLB." **Revisit:** (1) *why* those level-1 players have NULL league id (free agents? retired? `/players` gaps? — `League ID=0` means FA/retired per the API, distinct from a true NULL); (2) whether refresh can backfill `player_league_id` for level-1 players from team→league mapping (via `/lgdata` team membership) so the allowance can be tightened to strict `= primary`; (3) whether a co-resident *minor* league could also slip through. Low impact today (5 stragglers, negligible vs a 715-player primary sample), but worth a clean resolution. **LOE: Low-Medium.**

- [ ] **Role-dependent stuff (SP↔RP) in pitcher evaluation** — Investigate Session 89 (user-flagged). OOTP dynamically adjusts a pitcher's **Stuff** rating by role: a reliever leans on his best 1-2 pitches in short bursts, so a *concentrated* arsenal gets a **stuff boost as RP** (and a drop as SP); a *flat* arsenal sees little/no change. The magnitude tracks the quality gap between the top pitches and the rest. The ratings export gives us only ONE `Stf` value — the player's **currently-listed role** — but the individual pitch ratings (`fst`, `sld`, `cutt`, …) are role-independent, so the counterfactual stuff can be modeled. **The problem:** when our `assign_bucket` evaluates a listed-SP as our RP bucket (low-stamina reroute) or vice-versa, we feed it the stuff for the *wrong* role — over/under-rating those pitchers app-wide (composite/ceiling/FV/surplus/rankings/trades). Confirmed cases (vMLB): Levinson 45 SP→↑ as RP (FST 55/CUTT 55, CHG lags); Hudson 60 RP→55 SP (FST 80/KNCRV 70, big dropoff); Callen 45 SP→55 RP; Donovan no change (flat FST 50/SNK 40/CHG 35/CUTT 45). **Plan:** (1) reverse-engineer OOTP's SP↔RP stuff delta from arsenal shape (top-2-pitch quality vs arsenal mean) across a large role-split sample; (2) decide canonical representation — evaluate at a role-normalized baseline and apply the RP boost only when we bucket RP (derive the counterfactual from the arsenal, since one export can't give both); (3) apply in the evaluation engine where eval-bucket ≠ listed-role; (4) validate against the named cases + broad spot-check, re-run fv_calc, sanity-check re-bucketed pitchers move sensibly. **Core-model change — affects all pitcher valuation. Own scoped session. LOE: Medium-High (derivation + validation).**


- [ ] **Development speed metric — v1 shipped Session 88 (display); model integration remaining** — `evaluation/dev_speed.py` (pure) + `dev_speed` table (written by `fv_calc`), displayed on the player page (summary badge + Development-tab detail panel), league prospect lists, and team farm. Component-split (bat for hitters, composite for pitchers), per-(dev-group SP/RP/C/HIT, age-band, league) longitudinal z, POT-gap qualifier + ΔComposite/ΔCeiling decomposition (uses our composite/ceiling, not game OVR/POT), confidence tier + history gate. Spec: `.kiro/specs/development-speed-metric/design.md`. **Remaining (deferred, gated on multi-season `ratings_history` for benchmarking — PPL is the target league):** (1) risk-label proxy-swap (replace `stat_risk_modifier` with a dev-speed modifier — swap, not stack); (2) career-outcomes scenario-probability shift via `_adjust_scenario_probs`; (3) minor-league affiliate roster surface (`team_minor.html`); (4) coaching/level aggregation (exploratory, last). **Never** blend dev-speed directly into FV/surplus. **LOE: Medium (per remaining piece).**lopment for that bucket+age in this league. Reuses the machinery we already calibrate per league: the gap-closure-rate / age-runway / expected-gap tables (`calibrate.py` → `model_weights.json`, loaded by `fv_model.py`) and longitudinal `ratings_history` snapshots (monthly OVR/POT). Sketch: for a player, compare observed rating movement over a window against the league's expected movement for their bucket+age band → a z-score / ratio ("developing faster/slower than peers"). Feeds candidate uses: prospect risk refinement, a "⚡ Fast Track / ⚠ Stalled" pace badge (see the existing "Development pace indicator" task under Player Page — this is the underlying metric for it), farm-system-health signals, and draft/trade context. **Open questions:** (1) window length + how to handle sparse history (coverage drops with age — see the survivorship-bias note); (2) normalize on current-rating gains, POT-gap closure, or both; (3) cross-sectional vs longitudinal expectation baseline (longitudinal is cleaner but needs accumulated multi-season snapshots — same data constraint as the survivorship task); (4) how to present (raw metric vs badge). **LOE: Medium-High (metric design + per-league calibration + validation).**


- [ ] **Composite ↔ projection alignment (stat-blend calibration)** — Investigated Session 85. The composite (ranking) and the projected WAR (production) tell *contradictory* stories for a third of proven MLB players: composite is ~pure tools while the projection is ~pure stats, because the two use independent, opposite-extreme stat-blending systems. Concretely: (1) `compute_composite_mlb`'s stat blend is effectively inert — only **3%** of players have `composite != tool_only` and by ±small amounts; (2) `stat_confidence` (in `player_value.py`) ramps to **1.0** at 120 IP / 400 PA, so the projection fully discards tools for anyone with a track record. Result: **33% of proven MLB players have a ≥1.0 WAR gap** between `tool_war` (composite-implied) and `peak_war` (projection) — e.g. Ricky Acevedo (comp 63, tool_war 8.1, proj 1.0), David McCullough (comp 58 RP, proj 0.1: stuff 65 but movement 45 / poor results — genuinely "overrated by tools," not unlucky). **Plan (user, Session 85):** *meet in the middle, data-driven* — strengthen the composite's stat blend AND cap/soften `stat_confidence` so the two converge on one story, with the blend values **derived from data** (not eyeballed) and **validated on the hold-out** (`model_regression.py --test stat_blend` / `holdout`), then **sample players to confirm alignment** rather than contradiction. Baseline: current stat_blend R² 0.079→0.103 (+0.024) on PPL — marginal, so there's headroom. Also consider: RP-specific higher IP-full-confidence threshold (120 IP is ~2.5 noisy RP seasons); surfacing expected-vs-actual (percentile/xERA) context on the player + FA views so the user can see *why* the numbers land where they do (unlucky vs overrated). Levers: `STAT_CONFIDENCE_IP_FULL/PA_FULL/EXPONENT` (constants.py), `compute_composite_mlb` blend weights (composite.py). Core-model change — affects rankings, projections, surplus, FV, trades app-wide. **LOE: Medium-High (calibration + validation).**

  **Session 93 measurement (concrete target added):** the run-space composite tracks actual MLB WAR/600 at **corr 0.62 on eMLB but only ~0.47 on vMLB and PPL** — a large league-to-league fidelity gap in the central ranking metric, meaning rankings/FV/surplus are meaningfully noisier in two of three leagues. This is the headline target for this project (diagnose *why* the gap — sample size, scale, stat-blend calibration, run-env). Composite tracks WAR slightly BETTER than raw total_runs (0.62 vs 0.56 eMLB), so the run→composite path is sound; the gap is elsewhere. Fold in the known-small residual-bat term (partial r 0.167) here as one small validated component. Possible contributor to investigate: per-position ZR fielding curves fit on thin samples (SS n=56) — check fielding-run sensitivity to calibration sample size.

  **Session 93 RESOLUTION — the same-year corr gap is a DATA CEILING, not a model defect. This specific sub-question is closed; the stat-blend alignment item above remains open on its own merits.** Diagnosis: (1) The composite is at/above the OPTIMAL achievable linear tool-fit in every league (headroom eMLB +0.006, vMLB **−0.117**, PPL −0.037 — i.e. our composite BEATS a from-scratch regression of the tools, because it blends stats/position the tools alone miss). No reweighting can exceed this. (2) The lower vMLB/PPL same-year corr traces to **compressed source ratings** — normalized tool SDs are much tighter there (contact SD 5.4–6.4 vs eMLB 9.0), so OOTP's ratings carry less outcome information; that's an intrinsic ceiling. (3) The composite nonetheless ranks players **monotonically by actual WAR** in all three leagues (vMLB quintiles +1.34→+4.16, PPL +2.20→+5.52) — core ranking job is sound. (4) Against the target that matters — **next-year** WAR — composite crushes prior-year stats (vMLB 0.71 vs 0.42), confirming that leaning harder on stats would HURT projections. **Takeaway: don't chase the same-year corr number; it's noise + data ceiling. The real remaining gap is interpretability (see position-relative-context follow-up), not accuracy.**

- [x] **Per-tool, per-league transform curves** — Replaced the single hardcoded global `tool_transform` with per-tool, per-league curves derived from each league's marginal-WAR-by-rating-band data during calibration. `derive_tool_transform`/`apply_tool_transform` (pure, in `evaluation/composite.py`); `_calibrate_tool_transforms` fits residualized (multivariate-OLS) band means, shrinks toward per-tool priors by sample, enforces monotonicity, pins 50→0; stored under `tool_transforms` in `tool_weights.json`; threaded through composite AND ceiling functions (backward-compatible fallback to global transform). **Out-of-sample validated** (`model_regression.py --test holdout`, 4 hold-out years): hitters +0.046 R² every year (robust gain); pitchers −0.020 R² (accepted tradeoff — buys elite-arm ordering/ceiling sanity at a small WAR-prediction cost, since pitcher WAR here is movement-driven). **Done Session 82.**
- [ ] **Prediction-optimal pitcher transform (decision)** — The hold-out test showed the per-tool transform slightly *hurts* pitcher WAR prediction (−0.02 R²) while helping the eye-test ordering/ceilings. Open decision: apply the per-tool transform to **hitters only** (strictly prediction-optimal) and keep pitchers on the global/lighter curve, vs keep the full per-tool transform for the pitcher ordering benefits. Currently the full per-tool transform is applied to both. **LOE: Low.**
- [ ] **Re-run fv_calc + prospect surplus validation after the transform change** — The per-tool transform is a broad shift to ceilings (7,700+ prospects changed), and FV/surplus derive from ceilings. Re-run `fv_calc`, spot-check top-prospect FV against intuition, and re-run the prospect surplus validation before fully trusting the re-ranked farm system. **LOE: Low-Medium.**
- [ ] **Pitcher weight calibration — validate after next recalibration** — Session 82 dropped arsenal from the regression and added prior shrinkage; recalibrated on eMLB (RP stuff 0.07→0.18, SP 0.23→0.27; movement still leads per the data). Consider whether `derive_tool_weights` should move from per-feature r² to multivariate OLS (Session 82: OLS gives RP stuff β=0.30 vs r²'s 0.08 — r² starves correlated causal tools). The accepted movement-dominance means composite optimizes this league's WAR correlation, not necessarily causal pitching value. **LOE: Low-Medium.**

- [x] **Score compression further tuning** — Replaced elite tool bonus with piecewise tool transform (Session 47). Peak tool bonus for ceiling added (Session 48). 1.2× above-60 bonus restored (Session 52) to decompress top end. Composite range now 41-80 on eMLB, 37-76 on VMLB.
- [x] **Calibration on VMLB** — Full calibrate → evaluation_engine → fv_calc pipeline run on VMLB (Session 48). R²-blended defaults and raised min_weight floors produce stable cross-league weights (cosine similarity 0.98+).
- [x] **Carrying tool config review** — Percentile-based threshold (P85) now adapts to league distributions. VMLB calibrates all 7 positions (was only 2). Default merge fills gaps. Carrying tool bonus fully disabled (composite Session 56, ceiling Session 57) as redundant with tool transform (1.3× above 60) + peak tool bonus. **Done Session 57.**
- [x] **Tool interaction terms** — contact×eye, power×eye (hitters) and stuff×movement (pitchers) added Session 56, disabled Session 57. Were dead code (weights never written to `tool_weights.json`). Multivariate OLS shows residual correlation ~0.01 — collinear with individual tools (r=0.85-0.93), no explanatory power beyond linear model. Tool transform already captures the non-linearity. **Removed Session 57.**
- [x] **Stat blending: ERA- conversion and P95 calibration** — `_compute_stat_signal` now uses ERA- for pitchers (was FIP-). OOTP WAR is RA9-based; FIP systematically undervalued contact-management pitchers. P95 calibration still pending (slopes not yet wired). **Done Session 54 (ERA- part).**
- [x] **Evaluation model documentation** — `docs/evaluation_model.md` written with full pipeline diagram, formulas, constants, weight tables, and design rationale. **Done Session 52.**
- [ ] **SP underrepresentation in prospect rankings** — 18-20/100 SP in VMLB, 6/100 in EMLB. Investigated Session 49: OOTP generates tighter pitcher tool distributions (stuff/mov SD ≈ 11 vs hitter power SD ≈ 18), especially on 1-100 scale. HRA/PBABIP now included in pitcher composite for differentiation. VMLB representation is close to real baseball (~25-30%); EMLB is a league composition issue. **LOE: N/A — understood limitation.**
- [x] **SP FV undervaluation** — Investigated Session 50. Pitcher true_ceiling runs ~5-6 below game POT (e.g. 59 vs 65 for Schwarzenberg). Root cause: stuff rating already incorporates individual pitch quality, so the arsenal bonus partially double-counts. The 5-6 point gap is a stable, predictable offset that affects all SP equally. The FV system compensates through ceiling-credit — Schwarzenberg correctly grades at FV 50 Medium despite the ceiling compression. Increasing arsenal weight would risk double-counting. **Accepted as known limitation.**
- [x] **Per-league dynamic aging/development curves** — Gap closure rates, age runway tables, and expected gap tables now calibrated per league from cross-sectional OVR/POT data during `calibrate.py`. Stored in `model_weights.json`, loaded by `fv_model.py` with hardcoded VMLB-derived fallbacks. EMLB shows significantly higher closure rates than VMLB (e.g. hitter age 22: 0.91 vs 0.67). **Done Session 51.**
- [ ] **Calibration stability / cross-environment reproducibility** — Investigated Session 91. Per-league evaluation (composite/ceiling/FV/rank) can diverge across environments running identical code on the same league, because `tool_weights`/`tool_transforms`/`model_weights` are re-derived from each DB's accumulated data every refresh. Observed on PPL — David Monahan graded 38/74 FV 65 rank-1 on a heavily-refreshed env vs 39/69 FV 60 rank-7 on a lightly-refreshed one; this env's hitter tool-weight regression sample was only N=172 with just 4 `ratings_history` snapshots. **Not a bug** — small-sample calibration noise; the more-refreshed env is the more reliable, and a lagging env converges as refresh history accumulates. Committing per-league calibrated JSON was rejected (breaks the league-agnostic design); `league.db` sync is the way to align two environments. Possible improvements: (1) surface calibration confidence / sample-size in the UI so users know when grades are provisional, (2) a stronger prior-shrinkage floor for very small samples (Session 82 shrinkage exists but small retro leagues still wobble), (3) document that evaluation converges with refresh history. **LOE: Low-Medium.**
- [ ] **Cross-sectional survivorship bias** — Investigated Session 51. OOTP revises POT downward for players who fail to develop: only 22% of POT 50+ players at age 17 still have POT 50+ at age 26. Cross-sectional realization rates are inflated because busts appear as "low-POT, high-realization" players. Impact: gap closure rates (→ risk labels) are slightly optimistic for high-ceiling prospects. FV grade formula is unaffected (uses current snapshot only). Expected gap tables are correct for their cross-sectional use case. The bust_discount (0.55-0.85 by age, raised Session 52 from 0.30-0.60) partially mitigates. True fix requires longitudinal tracking across multiple seasons via `ratings_history`. Player coverage is near-complete at ages 17-21 (100%) but drops to 72% at age 26 due to unsigned players lacking ratings. **LOE: N/A until multi-season data accumulated.**
- [x] **FV 45+ inflation residual** — Now at ~10/org vs Fangraphs ~8.3. Accepted as a methodology difference: our tool-based composite is intentionally more generous than the game's OVR for developing players. The sub-MLB floor penalty (Session 51) reduced it from ~11 to ~10. Further reduction would require penalizing unproven tools, which conflicts with the design principle that composite = pure tool value. **Accepted at current level.**
- [x] **Composite-OVR divergence investigation** — Prospect composites run +7-8 above OVR. Investigated Session 51: the divergence IS the development gap — the difference between what tools say and what the player has proven. The game's OVR likely includes a "proven-ness" factor our composite intentionally omits. The sub-MLB floor penalty addresses the portion caused by disqualifying tool weaknesses. Remaining divergence is accepted as a design feature. **Closed — accepted as design choice.**
- [x] **COMPOSITE_TO_WAR calibration** — Now that composite scores exist on both leagues, run a second calibration pass to produce COMPOSITE_TO_WAR tables. These feed into `peak_war_from_score()` for surplus calculations. **Done Session 48.**

### FV Pipeline Migration to Composite/Ceiling Inputs

Legacy components designed for OVR/POT that need updating for composite/ceiling:

- [x] **`dev_weight()` curve tuning** — diff>=2 now gets 0.60 (was 0.50), rookie cap raised to 0.55. Fixes Joe Read-type undervaluation. **Done Session 49.**
- [x] **`effective_pot()` override removal** — Was dead code (column name mismatch). Removed from calc_fv. **Done Session 49.**
- [x] **`RP_POT_DISCOUNT` (0.8×) review** — Reduced to 0.85×. Old value double-counted RP devaluation in pitcher composite weights. FV 57 cap still limits top end. **Done Session 49.**
- [x] **Prospect discount + dev_weight interaction** — Resolved by dev_weight fix. Discount impact on FV is now ~2 points (acceptable). **Done Session 49.**

---

## Model & Data

- [x] **Arb salary calibration broken** — Fixed Session 56. Added WAR ≥ 1.0 floor, outlier cap, N ≥ 10 minimum, monotonic enforcement. Switched from flat-percentage model to raise-based (arb_model.arb_salary). Fixed discount mismatch (salary now discounted same as value). EMLB: {1:0.24, 2:0.24, 3:0.32}. **Done Session 56.**
- [x] **Arb salary constants recalibration** — Perpetual arb leagues now use a growth+ceiling model: `salary = min(min_sal + k × max(0, career_WAR - discount)^exp, ceiling_pct × WAR × $/WAR)`. Calibrated per league from actual 1-year contract data during `calibrate.py`. FA leagues remain on the existing exponential model (unchanged). **Done Session 60b.**
- [x] **Prospect WAR projection methodology** — Current approach uses `FV_TO_PEAK_WAR` lookup table (FV 50 → 2.0 WAR) with aging curve and ramp. Investigated Session 56: using ceiling→WAR would double-count upside (FV already encodes development probability). FV→WAR is correct in principle. Now passes continuous FV (pre-rounding) to `prospect_surplus` for interpolation within tiers. **Done Session 57.**
- [ ] **`estimate_control()` rounds service time incorrectly** — Uses `math.ceil(svc)` to convert fractional service years into an integer for the `6 - est_svc` remaining-control calculation. A player with 4.70 service years gets ceil'd to 5, yielding 1 year of control — but they actually have 2 arb years remaining (years 5 and 6). Should use `int(svc)` (floor) or `math.floor(svc)` instead. Impact: surplus for arb-eligible players with fractional service ≥ X.5 is understated by one full year of control. Affects the direct `estimate_control` callers — `contract_value.py`, `team_queries.py`, `projections.py` (NOT `player_evaluation.surplus`: `fv_calc` reads control from the contract for above-min players and only uses the service helper for near-min pre-arb deals — verified by re-running fv_calc). Discovered via Pat Pethel (4yr + 809 days → 4.70 svc → incorrectly shows 1yr control instead of 2). **Fixed Session 84** — all service-time interpretation consolidated into a single `arb.service_time()` helper (`ServiceTime` dataclass); control now uses `completed_years` (floor). Confirmed field semantics: `mlb_service_days` is cumulative total, full year = 172 days, so `completed_years = days // 172`. **Done.**
- [ ] **Super Two arbitration not modeled** — Arb eligibility uses a flat 3 completed years everywhere. Real MLB/OOTP grants the top ~17% (configurable) of players with 2.xxx service an extra (4th) arb year, a season early. Detecting it requires ranking the whole 2.xxx-service cohort against a league-configurable cutoff each offseason — it cannot be read from one player's fields (`has_received_arbitration` only flips *after* the first arb, too late to catch the player whose upcoming offseason is his Super Two year). Consequence: a minority of 2.xxx players get their first arb year projected one season late (payroll slightly understated / surplus slightly overstated). Approach if implemented: cohort ranking in `fv_calc`/refresh with a league cutoff param, flag Super Two → `pre_arb_left = 0` + 4th arb year in `estimate_control`. **Low impact. LOE: Medium.**
- [ ] **Platoon exposure modeling** — Current composite uses overall ratings; FV platoon penalty (-2/-3) partially addresses severe splits. Full platoon modeling would value platoon contributors in context (e.g., a LHH with 70 contact vs RHP and 30 vs LHP has real value as a platoon piece). Requires research: how to weight L/R splits, what threshold defines "platoon only," how to reflect platoon value in surplus. **LOE: Medium-High.**
- [x] **Unified evaluation model** — Single player value model (`evaluation/player_value.py`) replaces the old dual-system. Smoothly transitions from tool-based to stat-based projection via `stat_confidence` ramp. Legacy tables (`prospect_fv`, `player_surplus`) are now SQL views on `player_evaluation`. Web layer fully decoupled from `contract_value.py` and `prospect_value.py`. Design: `docs/unified_evaluation_design.md`. **Done Session 78. Remaining: parameter calibration from data (separate task).**
- [x] **Historical fielding stats** — Refresh now fetches fielding in the historical stats loop (up to 15 years) with a backfill pass for leagues that already had batting but were missing fielding. **Done Session 63.**
- [x] **Surface tool_only_score on MLB player pages** — MLB players now show pure tool score in parentheses next to the stat-blended composite. **Done Session 51.**
- [x] **Risk labels in prospect list templates** — Risk initials with color coding now rendered in league prospect list, team org overview, and team farm top 15. **Done Session 51.**
- [ ] **Projection model reuse** — `projections.py` now has calibrated OPS+, ERA/FIP, WAR, and ratings interpolation models. Explore using these in: (1) prospect pages — show projected MLB stat lines at current and future development stages, (2) draft evaluation — project draft prospect ratings into expected MLB production, (3) trade calculator — replace or supplement surplus model with projected stat lines for more intuitive valuation, (4) farm analysis — add projected stat context to scouting summaries. **LOE: Varies per integration.**
- [ ] **Org overview scaffold** — automate the org overview report template pulling farm summary, roster summary, contracts, surplus rankings, extension priorities. Lower priority — report structure changes between evaluations.
- [ ] **Surplus model validation suite** — systematic validation of prospect and contract surplus models against real league data. Subtasks: (1) sanity-check top/bottom 25 prospects by surplus — do rankings match intuition? (2) cross-position trade equivalence — test model-fair swaps (FV 55 SP vs SS, FV 50 C vs 1B, elite RP vs mid SP) for smell test; (3) prospect→MLB crossover — compare AAA prospect value to same player's contract value after debut, check for discontinuities; (4) age sensitivity — verify younger prospects properly valued over older ones at same FV/Pot; (5) validate against actual league trades — run completed trades through calculator; (6) FA contract validation — compare $/WAR and market value projections to actual free agent signings; (7) SP/hitter arb salary spot-checks — extend the RP arb validation to other positions. **LOE: Medium.**

---

## Web UI — Team Page

- [x] **Stale players on org overview page** — Fixed: org overview queries now cross-reference `players.team_id/parent_team_id` to filter out traded/released players whose prior-year stats still reference the team. **Done Session 61.**
- [ ] **Playing time model edge cases** — current model is 77% within 100 PA, 92% within 200 PA across 4 test teams. Known gaps: (1) utility players with <3 games at every position get sprayed across the diamond via ratings fallback; (2) DH-primary detection uses 50% batting-fielding gap threshold — teams that rotate DH duties without a primary DH leave the slot empty; (3) bench players squeezed below realistic PA when starter is a multi-position player claiming the slot; (4) **global position optimization** — model assigns positions per-player, not globally. Would require a constraint-satisfaction or linear-programming approach to maximize total team WAR across all positions simultaneously. **LOE: Low-Medium per item, High for #4.**
- [ ] **Positional strength/weakness map** — starter Ovr + surplus vs league average at each position. CLI version done (`team_needs.py`). Web UI version (visual map on team page) still pending. **LOE: Medium.**
- [ ] **Pipeline view** — MLB starter → AAA depth → top prospect chain per position bucket. **LOE: Medium.**
- [ ] **Division rival comparison** — side-by-side surplus/farm/record for division teams. **LOE: Medium.**
- [ ] **Roster projection** — project next year's roster from contracts, arb estimates, FA departures. **LOE: High.**

---

## User Feedback (Beta Testers)

Items identified from beta tester usage and conversations.

### High Priority (Bugs)
- [x] **Org page — lineup card blank** — Starting lineup card on org overview shows blank for hitters (pitchers section was previously fixed). Root cause: `fielding_stats` empty for leagues onboarded mid-season (refresh only fetched current year). Fix: (1) refresh now always fetches prior-year fielding, (2) org overview falls back to `batting_stats` + `players.pos` when fielding is unavailable. Reported by Koba. **Done Session 62.**
- [x] **IP display uses float formatting instead of baseball notation** — Fixed Session 68. Applied `fmt_ip` filter to Jinja server-rendered view and added `fmtIp()` JS helper for client-rendered split view. Reported by Koba. **Done Session 68.**
- [x] **Split percentile qualification threshold too low** — Split PA/IP thresholds now scale with season progress (0.7×games for hitters, 0.25×games for pitchers). `pctile-unqualified` CSS class handles visual dimming for sub-threshold samples. **Done Session 75.**

### Feature Requests
- [ ] **CSV export for minor league rosters** — Extend CSV export to minor league team pages (individual affiliates) or create an "all minor leagues" org view with export. Enables external analysis of player development progression across the farm system. Reported by Koba. **LOE: Low-Medium. DONE.**
- [ ] **Development tab refinements** — (1) Rating change summary bullets alongside charts (like StatsPlus), (2) Level promotion markers on timeline x-axis, (3) Responsive chart width for narrow screens. **LOE: Medium.**
- [x] **CSV export from draft board** — Add export button to draft board that outputs the current filtered/sorted view as a CSV. Two use cases: (1) export remaining undrafted players for external LLM analysis, (2) export computed columns (FV, composite ceiling, surplus) to enrich external datasets. Should respect active filters (position, level, hide drafted). Reported by Koba. **Done Session 65.**
- [ ] **Split-based composite ratings on team roster** — Show composite/OVR recalculated using vs-LHP or vs-RHP ratings instead of overall ratings. On the team roster page, a "vR" / "vL" toggle would swap the rating column to show handedness-specific composites alongside the split counting stats. Enables platoon construction: identify which hitters are significantly better/worse against a specific hand. Requires: (1) evaluation engine to accept split ratings as input, (2) split stats (already fetchable via split_id 2/3) displayed alongside. Reported by Koba. **LOE: Medium.**
- [x] **Positional rankings: vs-average and component breakdown** — ±Avg column (vs positional median, color-coded green/red) for all positions. OFF/DEF component breakdown for hitter groups (uses actual positional defensive rating). Pitchers get ±Avg only. **Done Session 75.**
- [ ] **Custom pool / prospect explorer** — Allow users to upload or select a custom group of players (e.g., their full minor league system) and view them in a sortable/filterable table similar to the draft board. Users want to sort their org's players by specific tools, composite, ceiling, or FV without needing the draft pool context. Could reuse the draft board table component with a different data source. Reported by Koba. **LOE: Medium.**
- [ ] **Platoon finder** — Filter/search for players with significant platoon splits (strong vs LHP or vs RHP). Surface players meeting a platoon threshold across MLB and MiLB rosters, enabling platoon lineup construction. Criteria: large gap in L/R contact or power ratings (e.g., 15+ point split) AND the strong side is above average (55+). Display: list with name, bats, strong-side stats, weak-side stats, platoon label. Could live as a filter on the team roster page or as a standalone tool. Reported by Koba. **LOE: Medium.**
- [ ] **Fielding stats on evaluated position** — Show observed ZR, fielding %, and games played at each position the player has manned (from `fielding_stats` table). Displayed alongside the positional evaluation section so users can validate whether a position switch recommendation has real defensive track record behind it. Useful for utility players and position-change candidates. Reported by Koba. **LOE: Low-Medium.**

### Research / Model Tuning
- [x] **Prospect surplus inconsistency: fv_calc uses true_ceiling instead of OOTP Pot for scarcity** — Investigated Session 72. Using true_ceiling is correct: OOTP Pot changes over time (78% of players) and doesn't dictate sim outcomes (individual tools do). Our model's ceiling is the better production predictor. The actual issue was (1) the web UI recalculating surplus with rounded integer FV instead of `fv_continuous`, causing different numbers in header vs panel, and (2) the default scarcity table having breakpoints calibrated for the Pot scale. Fix: added `fv_continuous` column to `prospect_fv`, unified all calculation paths, recalibrated default scarcity table for composite ceiling scale. EMLB's calibrated table in `model_weights.json` was already correct. **Done Session 72.**
- [ ] **Extreme profile inflation** — Observation that players with one extreme tool + otherwise poor ratings (30/30/90 control pitcher, or 30 contact/90 power hitter) grade higher than balanced profiles in composite/FV. Other GMs tend to favor balanced players. The contact floor penalty partially addresses this for power-only hitters. Investigate: (1) do extreme profiles underperform their composite in-game? (2) would a general "tool imbalance" penalty be warranted? (3) could this be addressed via the existing draft settings sliders (contact floor, etc.) without formula changes? Reported by Koba. **LOE: Medium (research).**

### UX / Onboarding
- [x] **Draft pool import instructions** — Step-by-step OOTP export instructions now shown inline in the Upload Pool modal on the draft board. **Done Session 75.**

---

## Web UI — League Page

- [x] **Positional rankings page** — League-wide page showing top players by position group (C, IF, OF, SP, RP), split into MLB and prospect sections. For MLB: rank by composite/WAR. For prospects: rank by FV/surplus. **Already implemented.**
- [ ] **Power rankings trend indicators** — store historical rank snapshots (per eval_date or game_date) and show ▲/▼/— movement arrows next to rank. Needs: new DB table or JSON file for rank history, delta calculation. **LOE: Medium.**
- [ ] **League news / milestone ticker** — horizontal strip between standings and power rankings showing notable milestones (e.g. "Player X: 3 HR from 50"). Needs: milestone detection logic from stats. **LOE: Medium-High.**

### Draft Tab — Future Improvements
- [x] **Draft board: position filter breaks sorting** — Fixed: added `initSort(table)` to `sort.js` that rebinds click handlers after dynamic thead replacement. `renderDraftBoard()` already called it but the function didn't exist. **Done Session 61.**
- [x] **Draft prospect ranking** — sort by FV (primary) then surplus (secondary). Web UI already uses this sort. CLI `draft_board.py pick` uses draft value sort (FV + ceiling bonus + ctl penalty). **Done Session 53.**
- [x] **ADP and draft simulation** — Expected draft position based on POT rank. Draft sim with randomized other-team picks. Urgency-greedy list building for auto-draft upload. Org needs as tiebreaker. Web UI sim + upload buttons. **Done Session 55.**
- [ ] **"My List" draft board builder** — Sidebar panel where users build a ranked draft list by clicking prospects from the board. Features: (1) "Add to My List" button per prospect row, (2) reorderable list via drag-and-drop or up/down arrows, (3) localStorage persistence across sessions, (4) export as commissioner format (numbered list with game position + name) or StatsPlus upload format (plain text IDs, one per line). **LOE: Medium.**
- [x] **Visual flag badges** — Show Acc=L warning badge (⚠) and Extreme risk badge (☠) directly in the draft board table rows next to player name. **Done Session 57.**
- [x] **Sleeper/value flags** — Implemented via ADP system (Session 55). POT rank vs FV rank gap produces Sleeper/Value/Goes Early/Reach labels. Displayed in draft board table and CLI output. **Done Session 55.**
- [x] **Advanced filtering** — Collapsible tool filter panel on draft board with min-threshold inputs for potential tools (Con/Pow/Eye/Spd for hitters, Stf/Mov/Ctrl for pitchers) plus Pot and FV minimums. Filters apply in real-time. **Done Session 57.**
- [ ] **Post-draft grades** — team haul summaries after draft completion. **LOE: Medium.**
- [ ] **Bonus demand display + draft value integration** — Parse "Bonus Demand" column from uploaded draft pool CSV (if present). Store in `draft_pool.json` alongside player IDs. Display demand amount on the draft board table. Explore factoring demand into draft value calculations (over-slot demand reduces pick attractiveness, under-slot creates value). Would also inform auto-draft list generation — a player demanding $5M at a $2M slot is less draftable than their pure talent ranking suggests. **LOE: Medium.**
- [x] **Org needs for perpetual arb leagues** — Implemented weakness-based alternative: compares team's positional starter composite vs league median, gated by farm FV 50+ depth. SP uses 3rd-best starter (rotation depth indicator). Thresholds: +2 if ≥5 below median with no farm help, +1 if ≥2 below with no farm help, +1 if ≥8 below even with farm help. FA leagues unchanged (departure-based). **Done Session 60b.**
- [ ] **Positional versatility in depth calculations** — The draft board depth indicator (`get_draft_org_depth`) only counts players in their primary bucket, ignoring defensive versatility. A team with 5 SS/2B players who all have 65+ Pot3B shows "red" at 3B because nobody's *primary* bucket is 3B. Similarly, COF players with 50+ PotCF don't count toward CF depth. Fix: count players who qualify defensively at a position (e.g., pot rating ≥ 45) toward that position's depth, possibly at a discounted weight. This would give a much more accurate picture of true organizational depth at each position. **LOE: Medium.**
- [x] **League-aware defensive viability in bucket assignment** — `assign_bucket()` fallback now uses calibrated OLS positional models (R² 0.92-0.96) trained from each league's own defensive tool → positional rating data. Models predict estimated positional grades from IFR/IFA/IFE/TDP (infield), OFR/OFA/OFE (outfield), CArm/Blk/Frm (catcher), and Height (1B). Always uses potential ratings (age removed as factor). Stored in `model_weights.json` under `POSITIONAL_MODELS`, recalibrated during `calibrate.py`. **Done Session 66.**

---

## Web UI — Player Page

- [x] **Player development history chart** — SVG line charts showing rating trajectories over time using `ratings_history` snapshots. Tabbed by tool group (offensive, defensive, pitches). Current + potential lines with hover dots. **Done.**
- [x] **Percentile rankings: offseason display + multi-year history** — Percentile panel now auto-falls back to prior year during offseason. Year selector dropdown on all panels. New "Advanced" tab on player pages shows color-coded percentile history table (blue→white→red), fielding history by position, PA/IP context, career averages, value/percentile toggle. **Done Session 63.**
- [ ] **Level-relative ranking** — Show where a prospect ranks among all players at their level by OPS+/ERA-. Display as percentile bar ("Top 5% at level") on player page and as a column in prospect lists. Provides context for individual stat lines. **LOE: Low.**
- [ ] **Development pace indicator** — Compare a player's level progression to expected pace for their profile. Badges: "⚡ Fast Track" (1+ levels ahead), "📈 On Pace", "⚠️ Stalled" (1+ levels behind with high PA). Show on player page and farm overview. **LOE: Medium.**
- [ ] **Farm system health dashboard** — Aggregate MiLB stat signals at the org level: promotion-ready count, performance trajectory breakdown (rising/steady/falling), stats-vs-tools alignment chart. Enhances team farm page with system-level insight. **LOE: Medium.**
- [ ] **Historical prospect comparisons** — Show comparable players at the same age+level who went on to succeed/fail. "73% of similar performers reached MLB within 2 years." Requires accumulated multi-season data. **LOE: High.**

---

## Web UI — Navigation

- [x] **Minor league team pages** — ~~extend `/team/<id>` to affiliate team IDs~~ Done. Notable player filter tuned: "young for level" now requires ceiling ≥ 45 (was age-only). Fixes Intl/Rookie levels where every teenager qualified. Results: Intl 9.7→5.9/team, Rookie 6.2→4.7/team. Upper levels unchanged (~12-15/team, appropriate for prospect-heavy affiliates). **Done Session 57.**

---

## Web UI — Visual Overhaul

- [ ] **In-app help system** — Add contextual help for key concepts (FV, risk, composite, surplus, etc.). Options: (1) "?" icon next to metrics that opens a tooltip/popover with explanation, (2) a slide-out help panel accessible from the nav, (3) a glossary page. Current tooltips via `title` attributes cover basics; a richer system would improve onboarding for new users. **LOE: Medium.**
- [x] **Player page injury/status banner** — DL/60-Day DL/Day-to-Day/Injured/Out Indefinitely/DFA/Waivers classifications with color-coded banner. Data from `players` table. Team roster badges also fixed to distinguish DL vs INJ. **Done Session 73.**
- [ ] **Team logos** — add team logos to team pages and player pages. Source or generate logo assets for all 34 MLB teams. Display in page headers, standings, and anywhere team identity appears. **LOE: Low-Medium.**
- [ ] **UI overhaul exploration** — current layout is functional but generic. Investigate alternative visual styles, layouts, and design patterns to give the app more personality. **LOE: Medium-High.**

---

## Data & Research

- [x] **Team vs team history** — `get_head_to_head_matrix()` query function returns full NxN W-L matrix. Displayed on the league page Standings tab as a color-coded H2H grid. **Done Session 60b.**
- [ ] **Accuracy-scaled draft penalty research** — Investigate whether Acc=L players with elite ceilings (70+) hit at a higher rate than Acc=L players with modest ceilings. If so, scale the accuracy penalty in `draft_value()` by ceiling magnitude (e.g., `penalty = base × (1 - (ceiling - 55) * 0.02)`). Requires multi-draft historical data to validate. Context: eMLB 2033 draft analysis showed Gabriel Brown (Acc=L, 97 pow / 100 eye potential) was undervalued by our board due to flat -2 penalty. **LOE: Medium (data collection + validation).**
- [x] **SP stamina risk in draft valuation** — Investigated Session 60b. Data shows: (1) OOTP's SP/RP boundary is ~stm 40 in both EMLB and PPL; (2) low-stm SP who stay as starters produce ~17% less WAR/yr in EMLB (1.34 vs 1.62) but no penalty in PPL; (3) `assign_bucket()` already converts 35% of low-stm SP to RP bucket → -5 discount fires; (4) composite already penalizes stm<40 by -0.75; (5) stamina is developable for young pitchers. Conclusion: no `draft_value()` penalty needed — existing composite penalty + bucket assignment already captures the risk at appropriate magnitude. Additional penalty would double-count. **Closed — no change.**

---

## API Integration Roadmap

New StatsPlus API data discovered July 2026. Implementation plan in dependency order.
See `docs/client_reference.md` for full field documentation.

### Phase 1 — Expanded Player Fields ✅ (Completed Session 69)

The `/players` endpoint now returns 45 additional fields. Schema migration adds
30+ columns to `players` table; `_upsert_players()` stores all fields on refresh.

- [x] **1a. Injury status** — `injury_is_injured`, `injury_dl_left`, `injury_left`, `is_on_dl`, `is_on_dl60`, `dl_days_this_year`. DL/DFA/WVR badges on team roster pages. Trade targets shows 🏥 annotations and skips DFA'd players. `--exclude-injured` CLI flag.
- [x] **1b. Service time** — `mlb_service_years`, `mlb_service_days`, `mlb_service_days_this_year`, `pro_service_years`, `pro_service_days`. `arb_model.estimate_service_time()` reads exact values with games-based fallback. `free_agents.py` uses exact service for arb/FA classification.
- [x] **1c. Roster status flags** — `is_on_waivers`, `designated_for_assignment`, `is_on_secondary`, `free_agent`, `was_traded`, `days_on_waivers`, `days_on_waivers_left`, `has_received_arbitration`. DFA'd players filtered from trade targets.
- [x] **1d. Draft history** — `draft_year`, `draft_round`, `draft_pick`, `draft_overall_pick`, `draft_team_id`. Stored for 10,851 players.
- [x] **1e. Demographics** — `date_of_birth`, `weight`, `nation_id`, `uniform_number`. Stored alongside existing player fields.

### Phase 2 — Minor League Stats (Partially Complete)

Stats available for all minor league levels via `lid` parameter. Storage and refresh
pipeline complete; display and model integration remaining.

- [x] **2a. `/lgdata` client method** — `get_lgdata()` in client.py. Used during refresh to discover all league IDs and hierarchy. League structure stored in `league_settings.json`.
- [x] **2b. Schema: `league_id` column** — Added to `batting_stats`, `pitching_stats`, `fielding_stats`. NULL = MLB (backward compatible). MiLB rows use the league ID from `/lgdata`.
- [x] **2c. Refresh pipeline — MiLB stat pulls** — Fetches batting+pitching for all discovered MiLB leagues (13 leagues, ~10,600 rows total). Current year only. Adds ~15-20s to refresh time. Non-fatal on failure.
- [x] **2d. MiLB stats in prospect evaluation** — MiLB performance integrated into composite scoring (level-discounted blend), Performance-Adjusted Ceiling (PAC), and risk modifier. Calibrated from VMLB 2029-2034 regressions. Historical MiLB stats (5yr backfill) stored for multi-year analysis. **Done Session 74.**
- [x] **2e. MiLB stats on player pages** — Minor League Stats section on player page Stats tab. Batting and pitching tables with league names from `league_settings.json`. Hitter and pitcher pages both supported.

**✅ MiLB stats contamination — RESOLVED.** All MLB-only query sites use `mlb_*` views
(created Session 70). Full audit confirmed in Session 74: 100+ query sites verified,
evaluation engine, WAR model, calibration pipeline, and percentiles all correctly isolated.
One minor fix applied (fielding percentile year resolution).

**Known remaining issues:**
- Percentile rankings support per-level filtering (done Session 73)
- Offseason WAR projection still shows for completed season (display logic issue)

### Phase 3 — New Endpoints ✅ (3a, 3c completed Session 70)

Client methods implemented; trade block and standings integrated.

- [x] **3a. Trade block** — `trade_block` table populated during refresh. `trade_targets.py` shows 📋 annotation, `--on-block` flag filters to confirmed-available players.
- [ ] **3b. Ballparks** — `get_ballparks()` exists in client.py. Need: store park factors, use in stat normalization, display on team pages. **LOE: Low-Medium.**
- [x] **3c. League data (`/lgdata`) for standings** — `standings` table stores real W-L-GB for all teams. `_classify_sellers()` uses real wins. `standings.py` shows pythagorean + actual with delta.

### Phase 4 — Expanded Contract Data ✅ (Completed Session 70)

All 13 additional contract fields stored. Vesting options, buyouts, and incentives
integrated into trade targets, free agents, and player page contract display.

- [x] **4a. Option/vesting fields** — `last_year_vesting_option`, `next_last_year_team/player/vesting_option`, `last_year_option_buyout`, `next_last_year_option_buyout`. Trade targets shows VESTING status. Free agents shows buyout amounts.
- [x] **4b. Incentive fields** — `minimum_pa/ip` + bonuses, `mvp_bonus`, `cyyoung_bonus`, `allstar_bonus`. Player page contract data includes non-zero incentives dict.

### Phase 5 — OSA Ratings (Lower Priority)

- [ ] **5a. OSA ratings fetch** — Add `&osa=1` support to ratings export. Enables dual-rating comparison (scouted vs OSA) for accuracy assessment. Store in separate table or as additional columns. **LOE: Medium.**

### Implementation Notes

- Phase 1 complete — all player fields stored and used by downstream tools
- Phase 2 complete — MiLB stats ingested AND displayed on player pages. Model integration (2d) deferred.
- Phase 3a + 3c complete — trade block and real standings integrated into trade tools
- Phase 4 complete — all contract option/incentive fields stored and surfaced
- Phase 3b (park factors) and Phase 5 (OSA ratings) deferred — lower priority
- Refresh time: ~3:15 with MiLB stats + trade block + standings (was ~3:00). All new fetches non-fatal on failure.
- All changes maintain backward compatibility (NULL defaults for new columns, migrations idempotent)

---

## Performance & Code Quality

- [ ] **Page load performance optimization** — Pages are slow due to on-the-fly calculations (composite scores, surplus, percentiles, depth chart projections) on every request. Investigate and implement caching strategies: (1) pre-computed values stored in DB during refresh (already done for FV/surplus — extend to composites, percentiles, positional rankings), (2) Flask response caching (e.g., `flask-caching` with TTL tied to last refresh timestamp), (3) query-level memoization for expensive repeated calls within a single request, (4) identify the slowest pages/queries via profiling (`cProfile` or middleware timing) and target those first. Goal: sub-1s page loads for common pages (team roster, player page, prospect list). **Partial progress Session 76:** request-scoped connection sharing (via `_ScopedConnection` wrapper), `_get_state()` caching (16→1 file reads), `_get_eval_date()` caching (10→1 queries). **LOE: Medium (remaining items).**
- [x] **Codebase quality review & refactor** — Systematic audit and restructure into proper Python package (`src/statsplusplus/`). Complete: typed models, pure evaluation layer, config/client/data packages, web request-scoped context, Flask blueprints, CLI entry points, all shims eliminated. Package is fully self-contained. `scripts/` contains only CLI tools importing from the package. **Done Session 77.**

---

## Long-term

- [ ] **External data directory** — Move user data (`app_config.json`, league dirs, history) out of the install folder to the OS-standard location (`%LOCALAPPDATA%\StatsPlusPlus` on Windows, `~/.local/share/statsplusplus` on Linux, `~/Library/Application Support/StatsPlusPlus` on macOS). Enables zero-risk updates (delete old folder, extract new zip). Add "Data Location" display in Settings. Refactor `league_context.py` and launchers to resolve the external path. Currently all data lives in `data/` within the install — works but means updates require manually copying the data folder.
- [ ] **Phase 2 — Interactive tools** — trade workbench, prospect explorer, free agent planner. Trade analysis CLI toolset complete (Session 44): `trade_targets.py`, `trade_assets.py`, `team_needs.py`, `trade_calculator.py` improvements, `trade-analyst.md` agent. Remaining: web-based trade workbench UI, prospect explorer UI, FA planner UI.
- [ ] **Phase 3 — AI assistant (BYOK)** — Chat interface with league/team context, powered by the user's own LLM subscription. User links their API key (Claude, OpenAI, etc.) via Settings. We supply the agent system prompt (contextually loaded with league state, roster, farm, standings — similar to the existing `trade-analyst.md` / `beat-reporter.md` steering files). UI: chat bubble or slide-out panel available on any page. The agent acts as an assistant GM — can answer questions about the roster, suggest trades, evaluate prospects, explain metrics. **Open questions:** (1) How to authenticate — API key in settings vs OAuth vs browser extension proxy? (2) Context window management — how much league data to inject per conversation (full DB dump vs targeted retrieval)? (3) Tool use — can the agent call CLI tools server-side, or is it pure chat with pre-loaded context? (4) Cost transparency — users pay per-token, need visibility into usage. (5) Which providers to support first (Anthropic API is simplest, OpenAI similar, local models via Ollama?). **LOE: High (research + implementation). Feasibility study first.**
- [ ] **Discord integration** — ~~Set up a Discord channel for Stats++ users (development updates, feedback, feature requests).~~ Server created, webhook posting implemented (patch notes via `discord_post.py`), widget on settings page. Remaining: (1) inbound integration — create Discord bot application for read token, build `discord_sync.py` script to pull messages from #feature-requests and #bug-reports channels into structured session context, (2) explore slash commands for lightweight queries (/prospect, /standings). **LOE: Medium.**
- [ ] **Code architecture cleanup** — connection context manager, consistent row_factory, route-level error handling. Ongoing incremental work.
- [ ] **Finish web layer migration — two divergent Flask apps** — The live app is `web/app.py` (module-level `app`, full route set across `web/app.py` + `settings_routes.py` + `api_routes.py`, run via `python3 web/app.py`). The package has a second `create_app` factory in `src/statsplusplus/web/app.py` (the `spp-web` entry point) that registers `statsplusplus.web.routes.{team,league,player,settings,api}` blueprints — but those cover only a subset of the live routes and delegate back to the old `web/queries.py`/`web_league_context.py`. Running `spp-web` serves an incomplete app. Either finish porting all routes into the package blueprints and retire `web/app.py`, or drop the package factory and point `spp-web` at the live app. Discovered during the Session 80 entry-point audit. **LOE: Medium-High.**
- [ ] **Stat/ratings divergence flag** — surface confidence signal when `stat_peak_war` and `peak_war_from_ovr` differ by >1.5 WAR in trade calculator output. Player page already shows over/underperformance; this extends it to trade evaluation context.

- [x] **Comp-based FV validation tool** — CLI tool (`scripts/comp_validate.py`) finds MLB players with similar tool profiles and shows WAR distribution. Web UI shows "Ceiling Profile" summary on prospect pages. Uses potential ratings, rate-normalized WAR (per 600PA/180IP), year filtering. Data limitation: uses current ratings vs historical stats; most reliable for recent seasons. **Done Session 56.**
